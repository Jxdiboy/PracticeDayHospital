@{
    ViewData["Title"] = "Theaters";

}
@model List<DayHospital.Models.Admin.DayHospitalRecords>

<div class="col-12 grid-margin">
    <div class="card">
        <div class="card-body">
            <div>
                <h4 class="card-title text-center">Day Hospital Records</h4>
            </div>
            <div class="row">
                <div class="col-md-10">
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-11">
                    <div class="mt-3">
                        <div class="card-body">
                            <h3 class="card-title text-center"></h3>
                            <div class="table-responsive" style="height:500px;">
                                <table class="table table-hover mt-1 display" style="width:100%" id="td-table">

                                    <thead class="bg-light sticky-top">
                                        <tr>
                                            <th>Name</th>
                                            <th>Address</th>
                                            <th>Contact Number</th>
                                            <th>Email Address</th>
                                            <th>Purchase Manager Email Address</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model != null && Model.Count() > 0)
                                        {
                                            foreach (var item in Model)
                                            {
                                                <tr>
                                                    <td>
                                                        @item.Name
                                                    </td>
                                                    <td>
                                                        @item.AddressLine1
                                                    </td>
                                                    <td>
                                                        @item.ContactNumber
                                                    </td>
                                                    <td>
                                                        @item.Email
                                                    </td>
                                                    <td>
                                                        @item.PMEmail
                                                    </td>

                                                    <td class="text-center">
                                                        <a class="btn btn-outline-primary" type="button" href="@Url.Action("UpdateDayHospitalRecords","Admin", new {Id = item.Id})"><i class="fa-regular fa-pen-to-square"></i> Update</a>
                                                    </td>

                                                </tr>
                                            }

                                        }


                                    </tbody>

                                </table>
                            </div>

                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script src="https://cdn.datatables.net/2.0.4/js/dataTables.js"></script>
    <script>

        $(document).ready(function () {
            var table = $('#td-table').DataTable({
                "columnDefs": [
                    { "type": "string", "targets": 0 },
                    { "type": "string", "targets": 1 },
                    { "orderable": false, "targets": -1 }

                ]
            });
        });
    </script>
}
